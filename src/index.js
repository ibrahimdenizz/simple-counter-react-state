import React from 'react';
import { render } from 'react-dom';
import Index from './Hooks State/index';

const Application = () => {
  return <Index />;
};

render(<Application />, document.getElementById('root'));
